import 'package:flutter/material.dart';

class EvolutionTab extends StatelessWidget {
  const EvolutionTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Evolution'));
  }
}
